<div class="book-card">
    <img src="<%= book.image %>" alt="<%= book.title %> cover">
    <div>
        <a href="/book/<%= book.title %>"><%= book.title %> by <%= book.author %></a>
        <p><%= book.description %></p>
        <span><%= book.category %></span>
    </div>
</div>